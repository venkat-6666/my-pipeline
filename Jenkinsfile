pipeline{
    agent any
    environment {
        // Define the environment
        name = "venkat"
        course = "GCP"
    }
    stage{
        stage('Build'){
            steps{
                echo "Welcome ${name}"
                echo "your ${course}"
            }
        }
    }
}
