pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                sh "hostname -i"
            }
        } 
    }
        stages{
        stage("Test") {
        steps {
            echo "Testing the code...."
        }
        }
    }
}
