pipeline{
    agent any
    environment {
        // Define the environment
        name = "venkat"
        course = "GCP"
    }
    stages{
        stage('Build'){
            steps{
                echo "Welcome ${name}"
                echo "your ${course}"
            }
        }
    }
}
